import { Authenticator } from '@smartthings/core-sdk/dist/authenticator';
import { EndpointClientConfig, HttpClientHeaders, SmartThingsURLProvider, WarningFromHeader } from '@smartthings/core-sdk/dist/endpoint-client';
import { Logger } from '@smartthings/core-sdk/dist/logger';
export interface RESTClientConfig {
    logger?: Logger;
    loggingId?: string;
    version?: string;
    headers?: HttpClientHeaders;
    urlProvider?: SmartThingsURLProvider;
    locationId?: string;
    installedAppId?: string;
    warningLogger?: (warnings: WarningFromHeader[] | string) => void | Promise<void>;
}
export declare class RESTClient {
    protected static defaultHeaders: HttpClientHeaders;
    config: EndpointClientConfig;
    constructor(authenticator: Authenticator, config?: RESTClientConfig);
}
//# sourceMappingURL=rest-client.d.ts.map